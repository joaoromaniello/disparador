<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gerenciar Instâncias</title>
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="/index.css">
</head>
<body>
  <main class="container">
    <h1>Criar Nova Instância</h1>
    
    <form method="POST" action="/init" class="create-form">
      <input type="text" name="nome" placeholder="Nome da instância (ex: minha-loja)" required />
      <button type="submit">Criar</button>
    </form>

    <% if (usuariosConectados && usuariosConectados.length > 0) { %>
      <h2>Instâncias Conectadas</h2>
      
      <div class="table-container">
        <table>
          <thead>
            <tr>
              <th>Nome da Instância</th>
              <th>Número Conectado</th>
              <th>Token</th>
              <th>Ações</th>
            </tr>
          </thead>
          <tbody>
            <% usuariosConectados.forEach(u => { %>
              <tr>
                <td><%= u.instanceName %></td>
                <td><%= u.numeroConectado || 'N/A' %></td>
                <td>
                  <div class="token-cell" id="token-<%= u.instanceName %>">
                    <%= u.token %>
                  </div>
                </td>
                <td>
                  <div class="action-buttons">
                    <a href="/disparo?token=<%= u.token %>" target="_blank" class="action-button btn-primary">
                      Disparar
                    </a>
                    <button type="button" class="action-button btn-secondary copy-btn" data-token-id="token-<%= u.instanceName %>">
                      📋 Copiar
                    </button>
                  </div>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>
    <% } %>
  </main>
  
  <script>
    document.addEventListener('DOMContentLoaded', () => {
        const copyButtons = document.querySelectorAll('.copy-btn');

        copyButtons.forEach(button => {
            button.addEventListener('click', () => {
                const tokenId = button.dataset.tokenId;
                const tokenElement = document.getElementById(tokenId);
                const tokenText = tokenElement.innerText;

                // Usa a API do Clipboard para copiar o texto
                navigator.clipboard.writeText(tokenText).then(() => {
                    // Feedback para o usuário
                    const originalText = button.innerHTML;
                    button.innerHTML = '✅ Copiado!';
                    button.disabled = true;

                    setTimeout(() => {
                        button.innerHTML = originalText;
                        button.disabled = false;
                    }, 2000); // Volta ao normal depois de 2 segundos
                }).catch(err => {
                    console.error('Erro ao copiar o token: ', err);
                    alert('Não foi possível copiar o token.');
                });
            });
        });
    });
  </script>
</body>
</html>
